$(document).ready(function () {
     // status voucher
     let status_voucher = $('#status').val();
     // if(status_voucher != 'new' || status_voucher != 'active'){
     //      $('form[name="DetailView"] input:button[name="Edit"]').remove();
	// 	$('form[name="DetailView"] input:submit[name="Delete"]').remove();
     // }
});